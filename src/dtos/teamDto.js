/** Class representing a team. */
class team {
    /**
     * @param {string} code
     * @param {string} name
     */
    constructor({code, name}) {
        this.code = code;
        this.name = name;
    }
};

module.exports = team;
